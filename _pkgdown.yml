destination: docs

development:
  mode: auto

url: https://straw-boy.github.io/solvers/

reference:
- title: Main Functionality
  desc: Fit the model with `SLOPE()`, visualize the results with
        `plot.SLOPE()`, and produce predictions for new data with
        `predict.SLOPE()`.
